# スマモン（SmaMon）

作業の前に **[AGENTS.md](AGENTS.md)** を読んでください。
プロジェクトの概要・守ること・リリース手順・データの形・テストのやり方・
過去に踏んだ落とし穴を、そこにまとめてあります。

急ぎの人向けに要点だけ：

- **`main` へ直接コミット・プッシュする**（ブランチもPRも作らない）
- **修正したら必ず** `service-worker.js` の `CACHE_NAME`・`version.json`・
  `index.html` のバージョン表記と更新履歴の**4点をセットで更新する**
- 変更したJS/CSSは `index.html` の `?v=` も1つ上げる
- 回答は結論から、端的に。コメントもコミットメッセージも日本語で
- push が弾かれたら、依頼者がスタジオからコミットしている可能性がある。
  **force push はしない**。`git fetch` して rebase する
